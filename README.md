# Context-analyze

after cloning the repository, or zipping it and downloading it, 

go to the diectory, by (`cd Context-analyze`)
install deps by (`pip install -r req.txt`)
and then run the UI.py file by (`streamlit run UI.py`)

